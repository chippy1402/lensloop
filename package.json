{
  "name": "obsidian-lensloop",
  "version": "0.0.1",
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=dist/main.js --format=iife --target=es2020 --external:obsidian",
    "watch": "esbuild main.ts --bundle --outfile=dist/main.js --format=iife --target=es2020 --external:obsidian --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "obsidian": "^1.8.7",
    "typescript": "^5.3.3"
  }
}
